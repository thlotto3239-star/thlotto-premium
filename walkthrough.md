### 3. Login Page GPS Implementation
- Added High-Accuracy GPS prompt to `Login.jsx` for both standard PIN login and Google OAuth login.
- Ensures identical location tracking behavior during "เข้าสู่ระบบ" (Login) as implemented previously in "สมัครสมาชิก" (Register).
