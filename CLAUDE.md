# 🛑 CLAUDE / AI AGENT — STOP AND READ

**คุณกำลังทำงานบน TH-LOTTO Premium User App**

## 👉 อ่าน `README.md` เป็นไฟล์แรก ก่อนทำอะไรทุกอย่าง

ที่นั่นมี:
- กฎเหล็ก (NO CODE WITHOUT APPROVAL + GitHub safety)
- ลำดับการทำงานที่ถูกต้อง
- Project URLs (live + GitHub)
- โครงสร้างเอกสารที่ต้องอ่าน

## 🚨 กฎ 3 ข้อสำคัญที่สุด

1. **ก่อนแก้โค้ด** → สร้าง git tag checkpoint + push GitHub
2. **ก่อนเขียนโค้ด** → summary ให้ user approve ก่อน
3. **หลังแก้โค้ด** → update CHANGELOG.md + PROJECT_STATUS.md + commit + push + deploy + verify live

## 📚 เอกสารที่ต้องอ่าน (ตามลำดับ)

1. `README.md` — ทางเข้า + กฎเหล็ก
2. `AGENT_HANDOFF.md` — quick start
3. `DEVELOPMENT_GUIDE.md` — workflow
4. `PROJECT_STATUS.md` — สถานะปัจจุบัน
5. `CHANGELOG.md` — ประวัติเวอร์ชัน
6. `docs/INSTANT_LOTTERY_PLAN.md` — แผนหวย 1 นาที (มินิเกม)

## ⛔ ห้ามทำ

- แก้โค้ดโดยไม่สร้าง checkpoint tag ก่อน
- push ไป main ตรงๆ (ใช้ branch + PR)
- แก้ระบบหลัก (`bets`, `lottery_markets`, `Betting.jsx`) ถ้าไม่ได้รับคำสั่ง
- สร้าง project Vercel ใหม่
- สับสนระหว่าง User App vs Admin Panel (คนละ repo)

ทุกอย่าง: **ดูที่ `README.md`**
